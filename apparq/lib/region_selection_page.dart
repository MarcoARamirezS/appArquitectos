import 'package:flutter/material.dart';

class RegionSelectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Selecciona tu Región'),
      ),
      body: Center(
        child: Text('Selecciona tu región aquí'),
      ),
    );
  }
}